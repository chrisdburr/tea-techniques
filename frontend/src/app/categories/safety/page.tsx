import { CategoryPageTemplate } from "@/components/categories/CategoryPageTemplate";

export default function SafetyPage() {
  return (
    <CategoryPageTemplate
      goalName="Safety"
      goalDescription="Techniques that prevent harm to humans, environments, and other systems from AI operations. These approaches focus on identifying and mitigating potential risks, ensuring AI systems operate within safe boundaries and prevent unintended consequences."
    />
  );
}
